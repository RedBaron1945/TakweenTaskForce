{
  "entities": {
    "app_data": {
      "title": "AppData",
      "description": "Global synchronized app state including students and attendance records",
      "type": "object",
      "properties": {
        "students": { "type": "array", "description": "List of students" },
        "records": { "type": "object", "description": "All attendance & wird records" },
        "updatedAt": { "type": "string", "format": "date-time" }
      }
    },
    "access_vault": {
      "title": "AccessVault",
      "description": "Secure storage for supervisor access hash, protected from public reads",
      "type": "object",
      "properties": {
        "pinHash": { "type": "string", "description": "SHA-256 hash of the access code" },
        "updatedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["pinHash"]
    },
    "pin_verification": {
      "title": "PinVerification",
      "description": "Server-side verification attempt evaluated by Firestore security rules",
      "type": "object",
      "properties": {
        "token": { "type": "string", "description": "Client calculated token for server rule comparison" },
        "createdAt": { "type": "string" }
      },
      "required": ["token"]
    }
  },
  "firestore": {
    "/app_data/{docId}": {
      "schema": "app_data",
      "description": "Synchronized student and attendance records"
    },
    "/system_security/{docId}": {
      "schema": "access_vault",
      "description": "Zero-read access control vault storing access hash"
    },
    "/pin_verifications/{attemptId}": {
      "schema": "pin_verification",
      "description": "Verification attempts validated by security rules"
    }
  }
}
